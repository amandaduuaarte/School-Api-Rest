import homeController from '../../src/controllers/HomeController';

describe('Home Controller test', () => {
  it('Should send 200 for successful response', () => {
    // const sendSuccessResponse = homeController.index;
    // ToDo: Should send 200 for successful response when created new user
  });
});
